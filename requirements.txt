numpy==2.4.3
# torch==2.5.1+cu121 #for deployment on GPU
# torchaudio==2.5.1+cu121 #for deployment on GPU
# torchvision==0.20.1+cu121 #for deployment on GPU
torch==2.5.1 #for deployment on CPU
flask==3.1.3 #for deployment on CPU
